<%= link_to 'select item', new_item_path %>
<center>
<div id="post-form">
  <%= render partial: 'posts/new', locals:{post_obj: @post}%></div>

 <center>
<%  @users.each do |user| %>
      <%#= user.username %><br/>
 
  <% user.posts.each do |post|%>


      post: <%=post.post %><br>
      <% post.photos.each do |photo|%>
        <%= image_tag photo.photo_image.url ,width: '100x100'%>
      <%end%></br>
       <%=link_to "Like", post,method: :post ,remote: true %> 
       <%= link_to 'Destroy', post,:method => :delete, :data =>{:confirm => "Are you sure?"}, :class =>"delete_post" ,remote: true%></br>
   
        <% post.comments.each do |comment|%>
           <div class='delete_cmmnt'>
            commenter :<%= comment.user.username %><br/>
            comment :<%=comment.comment%> <br>          
              <%comment.photos.each do |photo|%>
                 <%= image_tag photo.photo_image.url ,width: '100x100'%><br>
               
              <%end%>
              <%#= link_to "Reply" ,new_comment_reply_path(post_id: post.id,comment_id: comment.id), remote: true %><br>
              <div id="reply-form"></div>
              <%= link_to 'Destroy', comment, :method => :delete, :data =>{:confirm => "Are you sure?"}, :class =>"delete_comment" ,remote: true%></br>
              </div>
        <%end%>
              <div id="comment-form">
              <%= link_to 'create comment', new_comment_path(post_id: post.id), remote: true%>
              </div><br>
                 
              <div id="comment"></div>
    <%#= link_to 'create comment', new_comment_path(post_id: post.id)%><br>
    <hr>
  
  <%end%>
<%end%>
</center>
</center>
</div>
