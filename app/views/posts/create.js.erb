$('#post-form').append("<%= j render(:partial => 'post_show', :locals => { :post => @post }) %>");

// $("#post-form").append("<%= @post.post %>: <%= image_tag @post.photos.first.photo_image.url %>");